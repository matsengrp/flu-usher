name: historydag

channels:
  - https://prefix.dev/conda-forge
  - https://prefix.dev/matsengrp
  - nodefaults

dependencies:
  # python is held at 3.10: historydag hits a closure bug under 3.12's
  # changed comprehension scoping.
  - python =3.10
  - historydag =1.3.1
